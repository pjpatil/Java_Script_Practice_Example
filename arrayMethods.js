let data = [2,5,7,8,9];

console.log(data.length);  // it show aaray length

console.log(data.push(10));  // is add data in last position

console.log(data.pop());      // is remove data in last element in the array

console.log(data.shift());   // it is remove fist element in array

console.log(data.unshift(10));    // is add fisst element in the aaray.
console.log(data);